---
title: "Teman"
description: "Kumpulan tautan teman dan referensi yang sering saya kunjungi."
---

export const site = {
  name: "Blog Gilang",
  desc: "Catatan santai soal web dan hal yang lagi dipelajari.",
  url: "https://gilanx.pages.dev",
  avatar: "https://github.com/gilanx04.png",
  email: "gilanxcheetz@gmail.com",
};

export const notes = [
  { title: "Saling menautkan", content: "Tambahkan blog ini di halaman tautanmu dulu, lalu kirim detail situsmu ke saya." },
  { title: "Pemeliharaan", content: "Kalau tautannya mati terlalu lama, mungkin akan saya hapus." },
  { title: "Konten", content: "Saya lebih suka situs personal, catatan teknis, atau konten orisinal yang aman dibaca umum." },
  { title: "Akses", content: "Sebaiknya situsmu sudah memakai HTTPS dan bisa dibuka dengan stabil." },
];

<div class="not-prose my-4">
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
    <div class="rounded-2xl border border-(--line-divider) overflow-hidden flex flex-col">
      <div class="p-5 sm:p-6 flex flex-col flex-1">
        <div class="flex items-center gap-4 mb-5">
          <div class="relative shrink-0">
            <div class="w-16 h-16 rounded-xl overflow-hidden ring-2 ring-(--primary)/20">
              <img src={site.avatar} alt={site.name} class="w-full h-full object-cover" />
            </div>
          </div>
          <div>
            <h3 class="text-lg font-bold">{site.name}</h3>
            <p class="text-xs text-neutral-500 dark:text-neutral-400 mt-0.5">{site.desc}</p>
          </div>
        </div>

        <div class="space-y-2.5 flex-1">
          {[
            { label: "Nama situs", value: site.name },
            { label: "Deskripsi", value: site.desc },
            { label: "Tautan situs", value: site.url },
            { label: "Avatar", value: site.avatar },
          ].map((item) => (
            <div class="flex items-center justify-between gap-2 rounded-lg bg-black/5 dark:bg-white/5 px-3 py-2">
              <div class="min-w-0">
                <p class="text-[0.65rem] text-neutral-400 dark:text-neutral-500 mb-0.5">{item.label}</p>
                <p class="text-xs font-medium truncate">{item.value}</p>
              </div>
              <button onclick={`navigator.clipboard.writeText('${item.value.replace(/'/g, "\\'")}')`} class="shrink-0 w-7 h-7 flex items-center justify-center rounded-md bg-black/10 dark:bg-white/10 text-(--btn-content) hover:opacity-80 transition-opacity cursor-pointer">
                Salin
              </button>
            </div>
          ))}
        </div>
      </div>
    </div>

    <div class="rounded-2xl border border-(--line-divider) overflow-hidden flex flex-col">
      <div class="p-5 sm:p-6 flex flex-col flex-1">
        <h3 class="text-lg font-bold mb-5">Ajukan tautan teman</h3>
        <div class="space-y-4 text-sm text-neutral-600 dark:text-neutral-400">
          <p>1. Tambahkan blog ini ke halaman tautanmu dulu.</p>
          <p>2. Kirim nama situs, deskripsi, tautan, dan avatar lewat komentar atau email ke <code>{site.email}</code>.</p>
          <p>3. Nanti saya cek, lalu saya tambahkan kalau sudah cocok.</p>
        </div>
      </div>
    </div>
  </div>

  <div class="mt-4 rounded-2xl border border-(--line-divider) overflow-hidden">
    <div class="p-5 sm:p-6">
      <h3 class="text-base font-bold mb-4">Catatan</h3>
      <div class="space-y-3 text-sm text-neutral-600 dark:text-neutral-400">
        {notes.map((item) => (
          <div class="flex items-baseline gap-2.5">
            <span class="w-1.5 h-1.5 rounded-full bg-(--primary) shrink-0 translate-y-[-2px]"></span>
            <p><strong class="text-neutral-800 dark:text-neutral-200">{item.title}</strong>: {item.content}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>
